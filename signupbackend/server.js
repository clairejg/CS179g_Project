const express = require('express')
const app = express()

//1.import express
//2. initiazl the express in the app variable.

app.listen(4000, () => console.log("JK: server is up and running at port 4000"))

// react app is running on port 4000 we designeted.
//=> call back function